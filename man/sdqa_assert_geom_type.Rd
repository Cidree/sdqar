% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdqa_assert.R
\name{sdqa_assert_geom_type}
\alias{sdqa_assert_geom_type}
\title{Assert that a spatial object contains only the expected geometry type(s)}
\usage{
sdqa_assert_geom_type(x, expected)
}
\arguments{
\item{x}{A spatial object. Supported classes: [`sf`][sf::sf],
[`SpatVector`][terra::SpatVector], or `duckspatial_df`.
[`SpatRaster`][terra::SpatRaster] is not supported.}

\item{expected}{A character vector of allowed geometry type names (e.g.
`"POLYGON"`, `c("POLYGON", "MULTIPOLYGON")`). Case-insensitive.}
}
\value{
Invisibly returns TRUE. Throws an error if any geometry type
  found in `x` is not in `expected`.
}
\description{
Checks whether every geometry type found in `x` is within `expected`.
The comparison is case-insensitive. Throws an error listing all unexpected
types found.
}
\examples{
library(sf)
nc <- st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)

sdqa_assert_geom_type(nc, "MULTIPOLYGON")
sdqa_assert_geom_type(nc, c("polygon", "multipolygon"))

\dontrun{
sdqa_assert_geom_type(nc, "POLYGON")
}
}
