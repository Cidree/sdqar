#' @param ... Two or more spatial objects (`op = "=="`), or exactly two objects
#'   (any other `op`). Supported classes: [`sf`][sf::sf],
#'   [`SpatRaster`][terra::SpatRaster], [`SpatVector`][terra::SpatVector],
#'   or `duckspatial_df`.
